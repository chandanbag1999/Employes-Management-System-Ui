const CONFIG = {
    // Api base url
    API_BASE_URL: "https://emp-mgmt-api.runasp.net/api",

    // LocalStorage key name
    STORAGE_KEYS: {
        TOKEN: "ems_token",
        USERNAME: "ems_username",
        ROLE: "ems_role",
    },

    // Roles match
    ROLES: {
        ADMIN: "Admin",
        USER: "User",
    },

    PAGE_SIZE: 10,          // Rows per table page
    TOAST_DURATION: 3500,   // Notification duration
};